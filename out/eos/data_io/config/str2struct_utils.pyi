from eos.data_io.config import CANMessenger as CANMessenger, DBConfig as DBConfig, Driver as Driver, RE_DBKEY as RE_DBKEY, RE_DRIVER as RE_DRIVER, RE_VIN as RE_VIN, TripMessenger as TripMessenger, TruckInCloud as TruckInCloud, TruckInField as TruckInField, can_servers_by_host as can_servers_by_host, can_servers_by_name as can_servers_by_name, drivers_by_id as drivers_by_id, trip_servers_by_host as trip_servers_by_host, trip_servers_by_name as trip_servers_by_name, trucks_by_id as trucks_by_id, trucks_by_vin as trucks_by_vin
from typing import Union

def str_to_truck(truck_str: str) -> Union[TruckInCloud, TruckInField]: ...
def str_to_driver(driver_str: str) -> Driver: ...
def str_to_can_server(can_server_str: str) -> CANMessenger: ...
def str_to_trip_server(trip_server_str: str) -> TripMessenger: ...
